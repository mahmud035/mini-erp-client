/**
 * Placeholder route for the scaffold. Confirms the app boots and that
 * Tailwind semantic tokens render. Real pages arrive in later batches.
 */
export function MiniErpPage() {
  return (
    <main className="flex min-h-svh items-center justify-center bg-background text-foreground">
      <h1 className="text-4xl font-semibold tracking-tight">Mini ERP</h1>
    </main>
  )
}
